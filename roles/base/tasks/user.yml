---
# gijs is my own existing user; we don't create users here.
# Just make sure the admin SSH public key is authorized.
- name: Ensure SSH authorized key for {{ admin_user }}
  ansible.posix.authorized_key:
    user: "{{ admin_user }}"
    key: "{{ admin_ssh_key }}"
    state: present
